{
  "name": "mutable-app-framework",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:facet": "node scripts/build-facet.mjs",
    "predev": "npm run build:facet",
    "prestart": "npm run build:facet",
    "predeploy": "npm run build:facet",
    "pretypecheck": "npm run build:facet",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@cloudflare/dofs": "file:./vendor/dofs",
    "@cloudflare/think": "^0.13.0",
    "@cloudflare/worker-bundler": "^0.2.1",
    "agents": "^0.17.3",
    "ai": "^6.0.225",
    "workers-ai-provider": "^3.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.110.0"
  }
}
